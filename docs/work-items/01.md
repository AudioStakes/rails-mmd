# 対応01：Default gateのRails matrix二重実行を解消

- 優先順位: 1
- 状態: 完了
- 題名: Default gateのRails matrix二重実行を解消
- 最終更新: 2026-07-19
- 元資料: docs/verification-performance-plan.md

## 元の指示

Remove the duplicate real Rails matrix execution from the default gate while
preserving coverage of the Rake wiring and all three runtime pairs.

## 完了条件

- Default Rake gateで全3 Rails matrix pairsが一度だけ実行される。
- Rake wiring、selector、prerequisite、artifact failureの回帰テストを保持する。
- cold/warmの同条件再計測でdefault gateのwall timeが明確に短縮する。
- 専門観点別レビューでCritical/Important指摘が解消される。
- fresh full verificationが成功し、改善値と成果物が記録される。

## 既存成果物の確認

- 2026-07-19: origin/main 1b978d9 のRakefile defaultは rubocop, spec, bundle:audit, coverage, verify:rails_matrix を順に起動する。RSpec内の実matrix exampleと後段taskが重複する。既存baselineはcold default 59.657秒、cold RSpec 46.556秒、cold matrix 39.292秒。

## 調査・知見

- 2026-07-19: Code-mapper確認: 重複はintegration specの全pair subprocess exampleとdefault task末尾のverify:rails_matrix。既存のdefault prerequisites assertionが配線を保証し、fake-asdf examplesがselector/prerequisite/artifact failureを保証する。全3実pairはdefault gate後段で一度実行される。最小安全変更は全pair subprocess exampleのみ削除。

## 機械化方針

- 2026-07-19: 既存のRake task、RSpec、RAILS_MMD_MATRIX_PAIR selectorを再利用する。新規依存は追加しない。入力はRakefileとintegration spec、出力は保持された回帰保証とwall-time計測。外部通信はbundle cache未構築時の既存bundle installのみ。失敗時はテスト失敗または改善率不足でコミットを停止する。

## Codexが行うこと

- 2026-07-19: 最小実装、専門レビュー修正、cold/warm再計測、full gate検証、明確な改善時のコミットをCodexが行う。

## 本人だけが行うこと

- 2026-07-19: なし。実装・検証・Git操作・PR作成は明示的にCodexへ委任済み。

## 必要データ

- 2026-07-19: 対象commit 1b978d9、docs/verification-performance-plan.mdのbaseline、Rakefile、tooling/rails_matrix.rb、spec/integration/rails_matrix_task_spec.rb。

## 実行記録

- 2026-07-19: 2026-07-19 baseline cold default: 61.060秒、264 examples/0 failures、3/3 matrix pass。spec/integration/rails_matrix_task_spec.rbの全pair実行exampleを削除し、default wiringと失敗経路specを保持。
- 2026-07-19: 2026-07-19 after-change controlled timings: cold default 51.910秒（baseline 61.060秒から9.150秒/15.0%短縮）、cold matrix 42.037秒、warm matrix 7.924秒、warm default 27.310秒、warm full spec 10.635秒。すべてexit 0。

## 成果物

- 2026-07-19: spec/integration/rails_matrix_task_spec.rb、docs/verification-performance-plan.md、docs/work-items/01.md

## 検証

- 2026-07-19: Targeted RSpec 16 examples/0 failures。Full RSpec 263 examples/0 failures。Cold default gateはRuboCop 82 files/no offenses、263 examples/0 failures、bundler-audit no vulnerabilities、Undercover pass、PASS 3/3 Rails matrix pairs。QA reviewerもreal matrixとtargeted specsを独立実行してpassを確認。
- 2026-07-19: cold default 51.910s vs 61.060s baseline (15.0% faster); targeted 16 examples/0 failures; full 263 examples/0 failures; RuboCop/audit/Undercover/3-of-3 Rails matrix pass; correctness/performance/QA reviews have no remaining findings

## 改善

- 2026-07-19: default gateの実matrixを2回から1回へ削減。cold defaultを15.0%短縮。RSpec standaloneはall-pair happy pathを実行せず、documented default/pre-push gateがsole real all-pair proofとなる。
